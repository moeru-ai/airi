# AIRI server WebSocket URL. Must match the URL Stage uses (see Stage connection settings).
# Default: same as typical AIRI dev server.
AIRI_URL=ws://localhost:6121/ws

# Optional: token for AIRI module authentication (if your server requires it).
# AIRI_TOKEN=

# OpenClaw gateway WebSocket URL. Default: OpenClaw's typical gateway port.
# Run OpenClaw with: openclaw gateway --port 18789 (or set this to your gateway URL).
OPENCLAW_GATEWAY_WS_URL=ws://127.0.0.1:18789

# Required if gateway uses a token (OPENCLAW_GATEWAY_TOKEN or openclaw gateway --token).
# Must match the gateway token or connect is rejected.
# OPENCLAW_AUTH_TOKEN=

# Gateway client id (must be from gateway allowlist). Default: gateway-client.
# Use openclaw-control-ui only when gateway allows Control UI bypass (see README).
# OPENCLAW_GATEWAY_CLIENT_ID=gateway-client

# OpenClaw session key. Default: main. Change if you use a different session for AIRI.
# OPENCLAW_SESSION_KEY=main

# 2026-05-15

- Repo branch context: user updated workspace to AIRI v0.10.2 and is working on branch `feature/tts-fishaudio` (worktree branch observed earlier as `feature/tts-fish-audio`; verify exact final branch name before commit-sensitive work).
- Kits API structure: plugin-facing discovery lives in `packages/plugin-sdk/src/plugin/apis/client/kits/index.ts` with `apis.kits.list()` and `apis.kits.getCapabilities(kitId)`; host validation/registry live in `packages/plugin-sdk/src/plugin-host/shared/kits.ts` and `packages/plugin-sdk/src/plugin-host/runtimes/shared/services/kits.ts`; Electron app wires built-in kits from `apps/stage-tamagotchi/src/main/services/airi/plugins/kits/`.
- TTS provider template choice: MiniMax Speech in `packages/stage-ui/src/stores/providers.ts` is the chosen reference for Fish Audio because it uses custom speech transport logic instead of a simple adapter.
- Fish Audio provider scaffold exists at `packages/stage-ui/src/stores/providers/fishaudio/index.ts` with registration in `packages/stage-ui/src/stores/providers.ts` and i18n labels added in `packages/i18n/src/locales/en/settings.yaml` and `packages/i18n/src/locales/zh-Hans/settings.yaml`.
- Fish Audio transport design: custom `/v1/tts` request mapping based on the MiniMax pattern; request `voice` maps to Fish Audio `reference_id`, `response_format` is respected, and the provider returns a normal audio `Response`.
- Tests/status: focused transport test added at `packages/stage-ui/src/stores/providers/fishaudio/index.test.ts`; targeted Vitest passed earlier, while full repo `pnpm typecheck` and `pnpm lint` still had unrelated pre-existing failures in `packages/plugin-sdk-tamagotchi` and `services/computer-use-mcp/src/desktop-grounding.ts`.
- Pending next change from user: make Fish Audio discovery dynamic in `packages/stage-ui/src/stores/providers/fishaudio/index.ts` by implementing static `listModels()` with `s2-pro` and `s1`, and `listVoices()` via `https://api.fish.audio/model?page_size=100` using `FISHAUDIO_API_KEY`, mapping Fish Audio `_id` -> `id` and `title` -> `name`, with matching test updates.

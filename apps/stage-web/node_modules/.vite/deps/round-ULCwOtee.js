//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/random.mjs
function random(minimum, maximum) {
	if (maximum == null) {
		maximum = minimum;
		minimum = 0;
	}
	if (minimum >= maximum) throw new Error("Invalid input: The maximum value must be greater than the minimum value.");
	return Math.random() * (maximum - minimum) + minimum;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/randomInt.mjs
function randomInt(minimum, maximum) {
	return Math.floor(random(minimum, maximum));
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/clamp.mjs
function clamp(value, bound1, bound2) {
	if (bound2 == null) return Math.min(value, bound1);
	return Math.min(Math.max(value, bound1), bound2);
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/inRange.mjs
function inRange(value, minimum, maximum) {
	if (maximum == null) {
		maximum = minimum;
		minimum = 0;
	}
	if (minimum >= maximum) throw new Error("The maximum value must be greater than the minimum value.");
	return minimum <= value && value < maximum;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/sum.mjs
function sum(nums) {
	let result = 0;
	for (let i = 0; i < nums.length; i++) result += nums[i];
	return result;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/mean.mjs
function mean(nums) {
	return sum(nums) / nums.length;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/sumBy.mjs
function sumBy(items, getValue) {
	let result = 0;
	for (let i = 0; i < items.length; i++) result += getValue(items[i], i);
	return result;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/meanBy.mjs
function meanBy(items, getValue) {
	return sumBy(items, (item) => getValue(item)) / items.length;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/median.mjs
function median(nums) {
	if (nums.length === 0) return NaN;
	const sorted = nums.slice().sort((a, b) => a - b);
	const middleIndex = Math.floor(sorted.length / 2);
	if (sorted.length % 2 === 0) return (sorted[middleIndex - 1] + sorted[middleIndex]) / 2;
	else return sorted[middleIndex];
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/medianBy.mjs
function medianBy(items, getValue) {
	return median(items.map((x) => getValue(x)));
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/range.mjs
function range(start, end, step = 1) {
	if (end == null) {
		end = start;
		start = 0;
	}
	if (!Number.isInteger(step) || step === 0) throw new Error(`The step value must be a non-zero integer.`);
	const length = Math.max(Math.ceil((end - start) / step), 0);
	const result = new Array(length);
	for (let i = 0; i < length; i++) result[i] = start + i * step;
	return result;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/rangeRight.mjs
function rangeRight(start, end, step = 1) {
	if (end == null) {
		end = start;
		start = 0;
	}
	if (!Number.isInteger(step) || step === 0) throw new Error(`The step value must be a non-zero integer.`);
	const length = Math.max(Math.ceil((end - start) / step), 0);
	const result = new Array(length);
	for (let i = 0; i < length; i++) result[i] = start + (length - i - 1) * step;
	return result;
}

//#endregion
//#region ../../node_modules/.pnpm/es-toolkit@1.43.0/node_modules/es-toolkit/dist/math/round.mjs
function round(value, precision = 0) {
	if (!Number.isInteger(precision)) throw new Error("Precision must be an integer.");
	const multiplier = Math.pow(10, precision);
	return Math.round(value * multiplier) / multiplier;
}

//#endregion
export { median as a, mean as c, clamp as d, randomInt as f, medianBy as i, sum as l, rangeRight as n, meanBy as o, random as p, range as r, sumBy as s, round as t, inRange as u };
//# sourceMappingURL=round-ULCwOtee.js.map
import { a as createImageProvider, c as createSpeechProvider, f as merge, n as createChatProviderWithExtraOptions, o as createModelProvider, r as createEmbedProvider, t as createChatProvider, u as createTranscriptionProvider } from "./utils-BJTkTswy.js";
import { r as objCamelToSnake } from "./dist-CwOhu_ZO.js";

//#region ../../node_modules/.pnpm/@xsai-ext+providers@0.4.3/node_modules/@xsai-ext/providers/dist/together-ai-SWwaUOgP.js
var createAbacus = (apiKey, baseURL = "https://routellm.abacus.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createAlibaba = (apiKey, baseURL = "https://dashscope-intl.aliyuncs.com/compatible-mode/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createAlibabaCn = (apiKey, baseURL = "https://dashscope.aliyuncs.com/compatible-mode/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createBailing = (apiKey, baseURL = "https://api.tbox.cn/api/llm/v1/chat/completions") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createBaseten = (apiKey, baseURL = "https://inference.baseten.co/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createCerebras = (apiKey, baseURL = "https://api.cerebras.ai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createChutes = (apiKey, baseURL = "https://llm.chutes.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createCohere = (apiKey, baseURL = "https://api.cohere.ai/compatibility/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createCortecs = (apiKey, baseURL = "https://api.cortecs.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createDeepinfra = (apiKey, baseURL = "https://api.deepinfra.com/v1/openai/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createDeepSeek = (apiKey, baseURL = "https://api.deepseek.com") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createFastrouter = (apiKey, baseURL = "https://go.fastrouter.ai/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createFireworks = (apiKey, baseURL = "https://api.fireworks.ai/inference/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createFirmware = (apiKey, baseURL = "https://app.firmware.ai/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createFriendli = (apiKey, baseURL = "https://api.friendli.ai/serverless/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createGithubCopilot = (apiKey, baseURL = "https://api.githubcopilot.com") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createGithubModels = (apiKey, baseURL = "https://models.github.ai/inference") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createGoogleGenerativeAI = (apiKey, baseURL = "https://generativelanguage.googleapis.com/v1beta/openai/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createGroq = (apiKey, baseURL = "https://api.groq.com/openai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createHelicone = (apiKey, baseURL = "https://ai-gateway.helicone.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createHuggingface = (apiKey, baseURL = "https://router.huggingface.co/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createIflowcn = (apiKey, baseURL = "https://apis.iflow.cn/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createInception = (apiKey, baseURL = "https://api.inceptionlabs.ai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createInference = (apiKey, baseURL = "https://inference.net/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createIoNet = (apiKey, baseURL = "https://api.intelligence.io.solutions/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createKimiForCoding = (apiKey, baseURL = "https://api.kimi.com/coding/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createLlama = (apiKey, baseURL = "https://api.llama.com/compat/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createLmstudio = (apiKey, baseURL = "http://127.0.0.1:1234/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createLucidquery = (apiKey, baseURL = "https://lucidquery.com/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMinimax = (apiKey, baseURL = "https://api.minimax.io/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMinimaxCn = (apiKey, baseURL = "https://api.minimaxi.com/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMistral = (apiKey, baseURL = "https://api.mistral.ai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createModelscope = (apiKey, baseURL = "https://api-inference.modelscope.cn/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMoonshotai = (apiKey, baseURL = "https://api.moonshot.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMoonshotaiCn = (apiKey, baseURL = "https://api.moonshot.cn/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createMorph = (apiKey, baseURL = "https://api.morphllm.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createNanoGpt = (apiKey, baseURL = "https://nano-gpt.com/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createNebius = (apiKey, baseURL = "https://api.tokenfactory.nebius.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createNovitaAi = (apiKey, baseURL = "https://api.novita.ai/openai") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createNvidia = (apiKey, baseURL = "https://integrate.api.nvidia.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createOllamaCloud = (apiKey, baseURL = "https://ollama.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createOpenAI = (apiKey, baseURL = "https://api.openai.com/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}), createImageProvider({
	apiKey,
	baseURL
}), createSpeechProvider({
	apiKey,
	baseURL
}), createTranscriptionProvider({
	apiKey,
	baseURL
}));
var createOpencode = (apiKey, baseURL = "https://opencode.ai/zen/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createOvhcloud = (apiKey, baseURL = "https://oai.endpoints.kepler.ai.cloud.ovh.net/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createPerplexity = (apiKey, baseURL = "https://api.perplexity.ai/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createPoe = (apiKey, baseURL = "https://api.poe.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createPrivatemodeAi = (apiKey, baseURL = "http://localhost:8080/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createRequesty = (apiKey, baseURL = "https://router.requesty.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createScaleway = (apiKey, baseURL = "https://api.scaleway.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createSiliconFlow = (apiKey, baseURL = "https://api.siliconflow.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}), createSpeechProvider({
	apiKey,
	baseURL
}), createTranscriptionProvider({
	apiKey,
	baseURL
}));
var createSiliconflowCn = (apiKey, baseURL = "https://api.siliconflow.cn/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createSubmodel = (apiKey, baseURL = "https://llm.submodel.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createSynthetic = (apiKey, baseURL = "https://api.synthetic.new/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createUpstage = (apiKey, baseURL = "https://api.upstage.ai/v1/solar") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createVenice = (apiKey, baseURL = "https://api.venice.ai/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createVivgrid = (apiKey, baseURL = "https://api.vivgrid.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createVultr = (apiKey, baseURL = "https://api.vultrinference.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createWandb = (apiKey, baseURL = "https://api.inference.wandb.ai/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createXai = (apiKey, baseURL = "https://api.x.ai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createXiaomi = (apiKey, baseURL = "https://api.xiaomimimo.com/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createZai = (apiKey, baseURL = "https://api.z.ai/api/paas/v4") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createZaiCodingPlan = (apiKey, baseURL = "https://api.z.ai/api/coding/paas/v4") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createZenmux = (apiKey, baseURL = "https://zenmux.ai/api/v1") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createZhipuai = (apiKey, baseURL = "https://open.bigmodel.cn/api/paas/v4") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createZhipuaiCodingPlan = (apiKey, baseURL = "https://open.bigmodel.cn/api/coding/paas/v4") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createStepfun = (apiKey, baseURL = "https://api.stepfun.com/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}), createSpeechProvider({
	apiKey,
	baseURL
}), createTranscriptionProvider({
	apiKey,
	baseURL
}));
var createTencentHunyuan = (apiKey, baseURL = "https://api.hunyuan.cloud.tencent.com/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createOllama = (apiKey, baseURL = "http://localhost:11434/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}));
var createLitellm = (apiKey, baseURL = "http://localhost:4000/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}), createSpeechProvider({
	apiKey,
	baseURL
}), createTranscriptionProvider({
	apiKey,
	baseURL
}));
var createAnthropic = (apiKey, baseURL = "https://api.anthropic.com/v1/") => merge(createChatProviderWithExtraOptions({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL
}));
var createFeatherless = (apiKey, baseURL = "https://api.featherless.ai/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL,
	fetch: async (...args) => globalThis.fetch(...args).then(async (res) => res.json()).then((data) => Response.json({
		data,
		object: "list"
	}))
}));
var createOpenRouter = (apiKey, baseURL = "https://openrouter.ai/api/v1/") => merge({ chat: (model, openRouterOptions) => {
	const requestOptions = {
		apiKey,
		baseURL,
		model
	};
	const toOpenRouterOptions = ({ extraHeaders, models, provider }) => {
		if (extraHeaders != null) {
			requestOptions.headers ??= {};
			Object.assign(requestOptions.headers, extraHeaders);
		}
		let transformedProvider;
		if (provider != null) transformedProvider = objCamelToSnake(provider);
		return objCamelToSnake({
			models,
			provider: transformedProvider
		});
	};
	return {
		...openRouterOptions ? toOpenRouterOptions(openRouterOptions) : {},
		...requestOptions
	};
} }, createModelProvider({
	apiKey,
	baseURL
}));
var createTogetherAI = (apiKey, baseURL = "https://api.together.xyz/v1/") => merge(createChatProvider({
	apiKey,
	baseURL
}), createEmbedProvider({
	apiKey,
	baseURL
}), createModelProvider({
	apiKey,
	baseURL,
	fetch: async (...args) => globalThis.fetch(...args).then(async (res) => res.json()).then((data) => Response.json({
		data,
		object: "list"
	}))
}));

//#endregion
//#region ../../node_modules/.pnpm/@xsai-ext+providers@0.4.3/node_modules/@xsai-ext/providers/dist/create.js
var createNovita = createNovitaAi;

//#endregion
export { createAbacus, createAlibaba, createAlibabaCn, createAnthropic, createBailing, createBaseten, createCerebras, createChutes, createCohere, createCortecs, createDeepSeek, createDeepinfra, createFastrouter, createFeatherless, createFireworks, createFirmware, createFriendli, createGithubCopilot, createGithubModels, createGoogleGenerativeAI, createGroq, createHelicone, createHuggingface, createIflowcn, createInception, createInference, createIoNet, createKimiForCoding, createLitellm, createLlama, createLmstudio, createLucidquery, createMinimax, createMinimaxCn, createMistral, createModelscope, createMoonshotai, createMoonshotaiCn, createMorph, createNanoGpt, createNebius, createNovita, createNovitaAi, createNvidia, createOllama, createOllamaCloud, createOpenAI, createOpenRouter, createOpencode, createOvhcloud, createPerplexity, createPoe, createPrivatemodeAi, createRequesty, createScaleway, createSiliconFlow, createSiliconflowCn, createStepfun, createSubmodel, createSynthetic, createTencentHunyuan, createTogetherAI, createUpstage, createVenice, createVivgrid, createVultr, createWandb, createXai, createXiaomi, createZai, createZaiCodingPlan, createZenmux, createZhipuai, createZhipuaiCodingPlan };
//# sourceMappingURL=@xsai-ext_providers_create.js.map
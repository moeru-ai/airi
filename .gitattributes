# Git LFS tracking for singing module test fixtures
# Root .gitignore globally ignores *.wav/*.mp3; LFS rules here ensure
# test fixtures in packages/singing are properly tracked.
packages/singing/test/fixtures/**/*.wav filter=lfs diff=lfs merge=lfs -text
packages/singing/test/fixtures/**/*.mp3 filter=lfs diff=lfs merge=lfs -text

/**
 * A plain-text multiline editor for keyboard targets that must not use a
 * browser form control. Enter submits the current value. Shift+Enter adds a line.
 */
export { default as BasicContentEditable } from './basic-content-editable.vue'

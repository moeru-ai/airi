# Sherpaw model catalogue

This package owns the model metadata shared by AIRI's Sherpaw runtime and build tooling. It does not download, install, or load model files.

Use it when code needs a stable model ID, supported languages, recognizer architecture, or pinned Hugging Face artifact URL. Do not put platform storage or Vite behavior here.

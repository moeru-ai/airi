/**
 * Owner key for every device-local record.
 *
 * Moeka keeps all user data on the device and serves no accounts, so one
 * identity owns chat sessions, character likes, and character bookmarks.
 */
export const LOCAL_USER_ID = 'local'

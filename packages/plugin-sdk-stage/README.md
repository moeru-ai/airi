# @proj-airi/plugin-sdk-stage

This package provides the stage-level APIs for AIRI extensions.

Use it to mount a gamelet or register model tools. The APIs work in Desktop, Web, and Pocket hosts.

Do not use this package for host-only filesystem access or platform IPC. Those boundaries stay in the host application.
